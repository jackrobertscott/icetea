describe('const route = Router.create()', () => {
  it.skip('should create a instance of Router');
  describe('const Routes = router.compile()', () => {
    it.skip('should create a react element');
  });
});
